# sigma-diagnostics

Shared Rust domain library for Sigma Racer diagnostics: DBC load/edit/export, frame decode/filter, MDF4 load, SocketCAN live capture, and updates catalog client.

Used by the `can-viewer` desktop app and intended for reuse by other firmware/UI crates (e.g. cluster) without Slint or session persistence.
